[profile.default]
src = 'src'
out = 'out'
libs = ['lib']
solc = "0.8.16"
optimizer = true
optimizer_runs = 1000000
via_ir = true

# Turn on more compiler settings here, specically the stack optimizer for via_ir process
